# 오늘뭐먹지 — Design System

> v0.1 · 2026-08-14
> 레퍼런스: [Litebox](https://litebox.ai/) (via UIUX Showcase)

---

## 1. 레퍼런스에서 가져온 것 / 버린 것

Litebox는 **다크 배경 + 네온 액센트** 기반의 에이전시 사이트다. 우리는 "밝고 식욕을 자극하는" 톤이므로, **색은 버리고 구조와 태도를 가져온다.**

| 요소 | Litebox | 오늘뭐먹지 |
|---|---|---|
| 배경 | 딥 블랙 | ✅ 대체 — 크림 화이트 `#FFF7EE` |
| 액센트 | 네온 라임·핑크·옐로 | ✅ 대체 — 토마토·탠저린·버터·바질 (음식 색) |
| 타이포 | 초대형 볼드 헤드라인, 타이트한 행간 | ⭕ **그대로 계승** |
| 히어로 | 루프 비디오 배경 | 🔄 번역 — 떠다니는 가게 카드 애니메이션 |
| 도형 | 알약·삼각형 등 추상 기하 도형 | 🔄 번역 — **같은 기하 문법으로 그린 음식 아이콘** |
| 프로세스 | 번호 매긴 단계 + 일러스트 | ⭕ **그대로 계승** |
| 케이스 캐러셀 | 클라이언트 작업 슬라이드 | 🔄 번역 — 취향 결과 카드 |
| CTA | 라운드 필 버튼, 반복 배치 | ⭕ **그대로 계승** |

**핵심 원칙:** 광고를 걸러주는 서비스가 광고처럼 보이면 안 된다. 화려하되 과장하지 않는다. 강조는 **색이 아니라 크기**로 한다.

---

## 2. 컬러

### 토큰

```css
/* Base */
--ink:        #221A14;   /* 본문·헤드라인 */
--ink-2:      #6E5F53;   /* 보조 텍스트 */
--ink-3:      #A2938700; /* 힌트 (alpha 사용) */
--cream:      #FFF7EE;   /* 페이지 배경 */
--peach:      #FFEADC;   /* 섹션 구분 배경 */
--white:      #FFFFFF;   /* 카드 표면 */

/* Accent — 음식에서 가져온 색 */
--tomato:     #E8400F;   /* Primary. 버튼·링크 등 텍스트 동반 */
--tomato-soft:#FF6B35;   /* 장식 전용 (텍스트 금지) */
--tangerine:  #FF9F1C;   /* 보조 강조, 도형 */
--butter:     #FFD34D;   /* 하이라이터, 도형 */
--basil:      #1F9D55;   /* 긍정·검증 신호 */
--line:       rgba(34,26,20,.10);
```

### 사용 규칙

- **Primary CTA는 `--tomato` 단 하나.** 페이지 안에서 색으로 경쟁하는 버튼을 만들지 않는다.
- `--tomato-soft`, `--butter`, `--tangerine`은 **도형·배경 전용**. 그 위에 텍스트를 올리지 않는다 (대비 미달).
- `--basil`은 "검증됨 / 통과"의 의미로만 쓴다. 장식으로 남발 금지.
- 배경 위계: `cream`(기본) → `peach`(강조 섹션) → `white`(카드). 3단계를 넘기지 않는다.

### 대비 체크

| 조합 | 비율 | 판정 |
|---|---|---|
| `--ink` on `--cream` | 13.9:1 | AAA |
| `--ink-2` on `--cream` | 5.4:1 | AA |
| `#FFF` on `--tomato` | 4.6:1 | AA (본문 크기 OK) |
| `--tomato` on `--cream` | 4.5:1 | AA |
| `#FFF` on `--tomato-soft` | 2.9:1 | ❌ 텍스트 금지 |

---

## 3. 타이포그래피

```css
font-family: 'Pretendard', -apple-system, 'Apple SD Gothic Neo',
             'Segoe UI', system-ui, sans-serif;
```

한글 헤드라인이 주인공이므로 **가변 폭 라틴 디스플레이 폰트는 쓰지 않는다.** Pretendard 한 벌로 900까지 밀어붙인다.

### 스케일

| 이름 | 크기 | 굵기 | 자간 | 행간 |
|---|---|---|---|---|
| Display | `clamp(2.6rem, 8vw, 6.5rem)` | 900 | `-0.045em` | 1.02 |
| H1 | `clamp(2rem, 5vw, 3.6rem)` | 800 | `-0.035em` | 1.12 |
| H2 | `clamp(1.4rem, 3vw, 2.1rem)` | 800 | `-0.025em` | 1.25 |
| Body-L | `1.125rem` | 500 | `-0.01em` | 1.65 |
| Body | `1rem` | 500 | `-0.005em` | 1.7 |
| Label | `0.8125rem` | 700 | `0.08em` (대문자) | 1.4 |

**규칙**
- 헤드라인은 **자간을 반드시 조인다.** 한글 900 웨이트는 기본 자간이 헐겁다.
- 헤드라인 줄바꿈은 의미 단위로 직접 끊는다 (`<br>` 또는 `text-wrap: balance`).
- 본문 한 줄 최대 **68자 (약 34em)**.
- Label(오버라인)은 대문자 라틴 + 자간 확장. 한글 Label에는 자간 확장을 적용하지 않는다.

---

## 4. 스페이싱 · 레이아웃

```css
--sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px; --sp-4: 16px;
--sp-5: 24px; --sp-6: 32px; --sp-7: 48px; --sp-8: 64px;
--sp-9: 96px; --sp-10: 128px;
```

- 컨테이너 최대 폭 **1200px**, 좌우 패딩 `clamp(20px, 5vw, 48px)`
- 섹션 상하 여백 `clamp(72px, 12vw, 160px)` — Litebox식 넉넉한 호흡
- 그리드: 12컬럼 / 모바일 4컬럼, gutter 24px
- **모바일 우선.** 사용 순간(점심 직전, 길 위)이 모바일이다.

### 라운딩 · 그림자

```css
--r-pill: 999px;   /* 버튼, 칩 */
--r-lg:   28px;    /* 카드 */
--r-md:   18px;    /* 인풋, 작은 카드 */

--shadow-sm: 0 2px 8px rgba(34,26,20,.06);
--shadow-md: 0 12px 32px rgba(34,26,20,.10);
--shadow-lg: 0 24px 64px rgba(232,64,15,.16);  /* 따뜻한 그림자 */
```

그림자는 회색이 아니라 **따뜻한 색을 섞는다.** 크림 배경 위의 중성 회색 그림자는 탁해 보인다.

---

## 5. 아이콘 언어 — 기하 도형을 음식으로

Litebox는 알약·삼각형·원 같은 추상 도형을 썼다. 우리는 **같은 기하학적 단순함을 유지하되 형태만 음식으로 바꾼다.**

### 규칙
- 획 두께 고정 **2.5px**, 라운드 캡
- 도형은 **원 · 반원 · 알약 · 물결선** 4종 조합으로만 그린다
- 한 아이콘에 색은 **최대 2개**
- 사실적 묘사 금지 — 이모지처럼 보이면 실패

### 세트

| 아이콘 | 구성 | 용도 |
|---|---|---|
| 🥣 **그릇** | 반원 + 타원 림 | 브랜드 마크, "한 끼" |
| 🍜 **면** | 그릇 + 물결선 3개 | 국물·따끈함 |
| 🌶 **고추** | 알약 회전 + 잎 | 매콤 취향 |
| ☕ **컵** | 사다리꼴 + 알약 손잡이 | 가볍게·카페 |
| ❤️ **하트** | 원 2개 + 삼각형 | 저장·단골 |
| ✓ **체크 배지** | 원 + 체크 | 검증된 리뷰 (basil) |
| ✕ **필터 배지** | 원 + X | 걸러진 리뷰 (ink-2, 저채도) |

### 배경 장식 도형
크림 배경 위에 `--butter`, `--tangerine`, `--tomato-soft`의 **큰 블러 원(blob)**을 낮은 투명도로 깔아 온기를 만든다. 콘텐츠 가독성을 해치지 않도록 `opacity ≤ 0.35`, `filter: blur(60px)` 이상.

---

## 6. 컴포넌트

### 버튼

```
Primary   배경 --tomato / 텍스트 #FFF / pill / 패딩 18px 34px / 700
          hover: translateY(-2px) + shadow-lg
Secondary 배경 투명 / 1.5px 보더 --ink / 텍스트 --ink / pill
Ghost     텍스트만 + 밑줄 애니메이션
```

버튼에는 `transition: 220ms cubic-bezier(.2,.7,.3,1)` 통일.

### 카드
`--white` 배경 + `--r-lg` + `--shadow-sm`. hover 시 `translateY(-6px)` + `--shadow-md`.

### 칩 (취향 선택지)
pill, 1.5px 보더. 선택 시 배경 `--tomato`, 텍스트 흰색, `scale(1.03)`.
**터치 타겟 최소 48px** — 모바일에서 빠르게 누르는 게 핵심 인터랙션이다.

### 티커 (마퀴)
Litebox의 에너지를 계승하는 장치. `--ink` 바탕에 크림 텍스트, 무한 스크롤. 문구는 서비스의 약속을 짧게 반복: `광고 없음 · 내 취향 · 도보 5분 · 오늘 점심`.

---

## 7. 모션

| 상황 | 처리 |
|---|---|
| 스크롤 진입 | `opacity 0→1`, `translateY 24px→0`, 600ms, stagger 80ms |
| 히어로 카드 | 6~8초 주기 상하 부유 (`translateY ±10px`) |
| 칩 선택 | 120ms scale 팝 후 다음 질문으로 자동 전환 |
| 버튼 hover | 220ms lift |
| 티커 | 30s linear infinite |

- 이징은 `cubic-bezier(.2,.7,.3,1)` 하나로 통일
- **`prefers-reduced-motion: reduce`에서 모든 부유·마퀴·자동 전환 정지** — 필수
- 패럴랙스 금지. 즉시형 서비스에서 스크롤이 무거우면 이탈한다.

---

## 8. 보이스 & 카피

- 사용자의 실제 말투를 그대로 쓴다: "오늘 뭐 먹지", "또 검색만 하다 끝났나요?"
- **금지어**: 찐맛집, 인생맛집, 성지, 강추 — 광고 리뷰의 언어다
- 숫자는 구체적으로: "30초", "도보 5분", "3초"
- 문장은 짧게. 헤드라인 15자 이내, 서브카피 2줄 이내

---

## 9. 접근성 체크리스트

- [ ] 모든 텍스트 대비 4.5:1 이상 (큰 텍스트 3:1)
- [ ] 포커스 링: `outline: 3px solid --tomato; outline-offset: 3px`
- [ ] 터치 타겟 48×48px 이상
- [ ] 온보딩 진행률을 `aria-valuenow`로 전달
- [ ] 장식용 SVG는 `aria-hidden="true"`
- [ ] `prefers-reduced-motion` 대응
- [ ] 색만으로 정보 전달 금지 — 검증/필터 배지는 아이콘 + 텍스트 병기
